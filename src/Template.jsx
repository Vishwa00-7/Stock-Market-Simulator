//For Template (Starter) for Other JSX Files
 
/*

//Importing Libraries

import React from "react";
import { useState , useContext } from "react";

//Global Variables
import { Global_Variables } from "./App.jsx";

//Styles
import styles from "./______.module.css";

//Importing Components

function _______(props){

    //const { ____ , ____ } = usseContext(Global_Variables);
    const AccountID = useContext(Global_Variables);
    const accountData = useRef(undefined);                      //Account Data of user

        useEffect(() => {
            if (Storage !== undefined) {
                accountData.current = localStorage.getItem(AccountID.current);
                accountData.current = JSON.parse(accountData.current);
                // console.log(accountData.current);
                }
        }, []);

    return (
        <div id={styles._____}>



        </div>
    );
}

export default ________;

*/